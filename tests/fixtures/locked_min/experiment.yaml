# Minimal locked experiment for Stage 1 packaging tests.
name: locked-min
inputs:
  structure: enantiomer_1.cif
  exp_data: exp_data.cif_pets
refinement:
  optimizer:
    name: lbfgs
  split:
    val_frac: 0.1
