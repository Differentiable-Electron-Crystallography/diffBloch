# Python / uv
.venv/
__pycache__/
*.py[cod]
.ipynb_checkpoints/

# Test / type / lint caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
pytest.xml

# Build / docs output
dist/
build/
docs/_build/

# Preprocess checkpoints (generated per-experiment; regenerated on demand, never committed)
plan.npz
plan.lock
# ...except the committed frozen checkpoints: the CI anchor fixture + the worked examples. These are
# deliberately versioned (reused release-gated) and regenerated on a __version__ bump.
!tests/fixtures/quartz_anchor/plan.npz
!tests/fixtures/quartz_anchor/plan.lock
!examples/experiments/quartz-checkpoint/plan.npz
!examples/experiments/quartz-checkpoint/plan.lock
!examples/experiments/abiraterone-checkpoint/plan.npz
!examples/experiments/abiraterone-checkpoint/plan.lock
!examples/experiments/quartz-chirality/matching-hand/plan.npz
!examples/experiments/quartz-chirality/matching-hand/plan.lock
!examples/experiments/quartz-chirality/opposite-hand/plan.npz
!examples/experiments/quartz-chirality/opposite-hand/plan.lock
# The verify-quartz-full stash: a from-scratch candidate checkpoint awaiting `just promote-quartz`
# (the explicit step that replaces the committed reference). Never committed itself.
.candidate/

# OS / editors
.DS_Store
.idea/
.ssec.env
